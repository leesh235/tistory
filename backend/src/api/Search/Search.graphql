scalar DateTime

type Search {
    postId: Int
    writer: String
    title: String
    createdAt: DateTime
}

type Data {
    SearchList:[Search]
}

type Query{
    getSearch(text: String count: Int! page: Int!): Response!
}

type Mutation{
 dummy: String
}