type Data {
    postId: Int
    writer: String
    title: String
    contents: Boolean
    createdAt: DateTime
}

type Query {
    getPostDetail(postId: Int!): Response
}

type Mutation {
    dummy: String
}