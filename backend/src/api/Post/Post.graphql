type Post {
    postId: Int
    writer: String
    title: String
    createdAt: DateTime
}

type Data {
    postList: [Post]
}

type Query{
    getAllPosts(count: Int! page: Int!): Response!
}

type Mutation{
    dummy: String
}