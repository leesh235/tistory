type Post {
    postId: String
    writer: String
    title: String
    createdAt: DateTime
}

type Response {
    posts: [Post]
    status: String
}

type Query{
    getAllPosts: Response!
}

type Mutation{
    dummy: String
}