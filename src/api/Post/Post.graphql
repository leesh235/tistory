type Post {
    postId: String
    title: String
    id: String
    createdAt: DateTime
}

type ResPosts {
    posts: [Post]
}

type Query{
    getAllPosts: ResPosts
}

type Mutation{
    dummy: String
}