type LoginInfo {
    email: String
    nickName: String
    userRole: String
    token: String
    status: String
}

type Mutation {
    login(email: String! password: String!): LoginInfo!
}

type Query {
    dummy: String
}