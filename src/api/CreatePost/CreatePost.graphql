type Response {
    postId: String
    status: String
    writer: String
}

type UploadTextResponse {
    check: Boolean
    status: String
}

type Mutation {
    createPost(title: String! contents: String): Response!
    uploadText(postId: String): UploadTextResponse!
}

type Query {
    dummy: String
}