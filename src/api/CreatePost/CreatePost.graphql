type PostInfo{
    postId: String
    writer: String
}

type Response {
    postInfo: PostInfo
    check: Boolean
    status: String
}

type Mutation {
    createPost(title: String! contents: String): Response!
}

type Query {
    dummy: String
}