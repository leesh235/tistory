type Post {
    title: String
    contents: String
    createdAt: DateTime
    id: String
    postImgId: String
}

type ConfirmInfo{
    equal: Boolean
    Post: Post
}

type Query {
    getPost(postId: String! id: String!): ConfirmInfo
}

type Mutation {
    dummy: String
}