type Post {
    postId: String
    writer: String
    title: String
    contents: String
    createdAt: DateTime
    postImgId: String
}

type Response{
    equal: Boolean
    status: String
    Post: Post
}

type Query {
    getPostDetail(postId: String! email: String!): Response
}

type Mutation {
    dummy: String
}