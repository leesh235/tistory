scalar DateTime

type Search {
    postId: String
    writer: String
    title: String
    createdAt: DateTime
}

type Response {
    search: [Search]
    postCnt: Int
    status: String
}

type Query{
    getSearch(text: String): Response!
}

type Mutation{
 dummy: String
}