scalar DateTime

type myPost {
    postId: String
    writer: String
    title: String
    createdAt: DateTime
}

type Response{
    status: String
    allMyPosts: [myPost]
}

type Query{
    getMyPosts: Response
}

type Mutation{
    dummy: String
}