import React from 'react';
import { ProfileContainer } from './ProfileContainer';

const ProfileIndex = () => {
        return(
                <ProfileContainer />
        );
}

export default ProfileIndex;