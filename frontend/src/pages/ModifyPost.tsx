import styled from 'styled-components';
import { ModifyPostContainer } from '../containers/ModifyPostContainer';

const Wrapper = styled.main`

`;

const ModifyPost = () => {
    return(
        <Wrapper>
            <ModifyPostContainer />
        </Wrapper>
    );
}

export default ModifyPost;