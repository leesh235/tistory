import styled from 'styled-components';

const Wrapper = styled.main`
    display: flex;
    flex-direction: row;
    width: 100%;
`;

const Profile = () => {
    return(
        <Wrapper>
            profile
        </Wrapper>
    );
}

export default Profile;